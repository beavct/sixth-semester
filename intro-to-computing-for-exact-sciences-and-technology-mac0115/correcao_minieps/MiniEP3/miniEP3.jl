function calculaArea(tipo, base, altura)
    #=
        calculaArea(tipo: Int64, base: Float64, altura: Float64) -> Float64
        Esta função deve calcular a área de um triângulo ou retângulo de acordo com os argumentos.
    =#

    return 
end

function calculaAreaTriangulo(base, altura)
    #=
        calculaAreaTriangulo(base: Float64, altura: Float64) -> Float64
        Esta função deve calcular a área de um triângulo.
    =#    

    return 
end

function calculaAreaRetangulo(base, altura)
    #=
        calculaAreaRetangulo(base: Float64, altura: Float64) -> Float64
        Esta função deve calcular a área de um retângulo.
    =#   

    return 
end

#=
    Você pode testar a função para diferentes valores de parâmetros
=# 

tipo = 1
base = 2
altura = 3
area = calculaArea(tipo,base,altura)

println("A área calculada é ", area)