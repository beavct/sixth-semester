function quaseIgual(num1, num2, epsilon)
    #=
        quaseIgual(num1: Float64, num2: Float64, epsilon: Float64) -> Bool
        Esta função deve verificar se a diferença entre os dois números está dentro da margem de erro
    =#   

    return 
end

resultado = quaseIgual(sin(pi/4), 0.7071, 0.0001)

println("O resultado foi ", resultado)